---
import { socialLinks, musicPlatforms } from '../data/socialLinks.js';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Sunkissed - Indie Rock Band" />
    <link rel="icon" type="image/png" href="/logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet" />
    <title>Sunkissed - Minimalist</title>
  </head>
  <body>
    <!-- Style Switcher -->
    <nav class="style-switcher">
      <span class="switcher-label">Style:</span>
      <a href="/vintage">70s Vintage</a>
      <a href="/minimalist" class="active">Minimalist</a>
      <a href="/dreamy">Dreamy</a>
      <a href="/grunge">Grunge</a>
      <a href="/">Original</a>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <div class="logo-container">
          <img src="/logo.png" alt="Sunkissed" class="logo" />
        </div>
        <h1>Sunkissed</h1>
        <p class="subtitle">Indie Rock</p>
        <div class="hero-actions">
          <a href="#music" class="link-button">Listen</a>
          <span class="divider">•</span>
          <a href="#about" class="link-button">About</a>
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section id="about" class="about-section">
      <div class="container-narrow">
        <div class="section-number">01</div>
        <h2>About</h2>

        <div class="about-content">
          <div class="image-wrapper">
            <img src="/cover_photo.jpeg" alt="Sunkissed Band" />
          </div>

          <div class="text-content">
            <p class="lead">
              sunkissed started as a move — not a band. Two friends packed up their lives in Toronto and landed in San Francisco.
            </p>

            <p>
              It wasn't until six months later—after soaking in the Californian sun and feeling the late-night breeze—the music started flowing. What emerged was a hazy blend of indie rock, surf-tinged guitars, dreamy bedroom pop, and the occasional psychedelic drift.
            </p>

            <p>
              Now a four-piece, sunkissed makes music that feels like sun-faded memories—nostalgic, a little restless, and entirely their own.
            </p>

            <div class="divider-line"></div>

            <p>
              Their debut single, "On the Sun Kissed Hills of San Francisco," is a raw, honest reflection on leaving Toronto for San Francisco, capturing the emotions felt while living in a new city.
            </p>

            <p>
              Their newest single, Echos, is an energetic track that contrasts the tension between the joy of experiencing something new with the nostalgia of letting go of the past.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Music Section -->
    <section id="music" class="music-section">
      <div class="container-narrow">
        <div class="section-number">02</div>
        <h2>Listen</h2>

        <div class="platforms">
          {musicPlatforms.map((platform) => (
            <a href={platform.url} target="_blank" rel="noopener noreferrer" class="platform-link">
              <span class="platform-name">{platform.name}</span>
              <svg class="arrow-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M7 17L17 7M17 7H7M17 7V17"/>
              </svg>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact-section">
      <div class="container-narrow">
        <div class="section-number">03</div>
        <h2>Connect</h2>

        <div class="social-links">
          {socialLinks.map((link) => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class="social-link">
              {link.name}
              <svg class="arrow-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M7 17L17 7M17 7H7M17 7V17"/>
              </svg>
            </a>
          ))}
        </div>

        <div class="newsletter">
          <h3>Newsletter</h3>
          <p>Stay updated on new releases and shows</p>
          <form action="https://formspree.io/f/YOUR_FORM_ID" method="POST" class="newsletter-form">
            <input type="email" name="email" placeholder="Email address" required />
            <button type="submit">Subscribe</button>
          </form>
        </div>
      </div>
    </section>

    <footer class="footer">
      <div class="container-narrow">
        <p>&copy; 2024 Sunkissed</p>
      </div>
    </footer>
  </body>
</html>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --black: #000000;
    --white: #FFFFFF;
    --gray-100: #F5F5F5;
    --gray-200: #E5E5E5;
    --gray-400: #999999;
    --gray-600: #666666;
    --accent: #000000;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background-color: var(--white);
    color: var(--black);
    line-height: 1.6;
    font-weight: 300;
    -webkit-font-smoothing: antialiased;
  }

  .container-narrow {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Style Switcher */
  .style-switcher {
    position: fixed;
    top: 30px;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 8px 16px;
    border: 1px solid var(--gray-200);
    display: flex;
    gap: 12px;
    align-items: center;
    z-index: 1000;
    font-size: 0.875rem;
  }

  .switcher-label {
    color: var(--gray-600);
    font-weight: 500;
  }

  .style-switcher a {
    color: var(--gray-600);
    text-decoration: none;
    padding: 4px 8px;
    transition: color 0.2s ease;
  }

  .style-switcher a:hover,
  .style-switcher a.active {
    color: var(--black);
  }

  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 2rem;
  }

  .hero-content {
    text-align: center;
    max-width: 600px;
  }

  .logo-container {
    margin-bottom: 3rem;
  }

  .logo {
    width: 200px;
    height: auto;
    opacity: 0.95;
  }

  .hero h1 {
    font-size: 4rem;
    font-weight: 300;
    letter-spacing: -0.02em;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.125rem;
    color: var(--gray-600);
    font-weight: 400;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin-bottom: 3rem;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
  }

  .link-button {
    color: var(--black);
    text-decoration: none;
    font-size: 1.125rem;
    font-weight: 400;
    position: relative;
    transition: opacity 0.2s ease;
  }

  .link-button::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--black);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .link-button:hover {
    opacity: 0.7;
  }

  .link-button:hover::after {
    transform: scaleX(1);
  }

  .divider {
    color: var(--gray-400);
    font-size: 1rem;
  }

  /* Section Styles */
  section {
    padding: 8rem 0;
  }

  .section-number {
    font-size: 0.875rem;
    color: var(--gray-400);
    font-weight: 500;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
  }

  h2 {
    font-size: 2.5rem;
    font-weight: 400;
    letter-spacing: -0.01em;
    margin-bottom: 4rem;
  }

  /* About Section */
  .about-section {
    background-color: var(--gray-100);
  }

  .about-content {
    display: grid;
    gap: 4rem;
  }

  .image-wrapper {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
  }

  .image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
  }

  .image-wrapper:hover img {
    transform: scale(1.05);
  }

  .text-content p {
    font-size: 1.125rem;
    line-height: 1.8;
    margin-bottom: 1.5rem;
    color: var(--gray-600);
  }

  .text-content .lead {
    font-size: 1.5rem;
    font-weight: 400;
    color: var(--black);
    line-height: 1.6;
  }

  .divider-line {
    width: 60px;
    height: 1px;
    background: var(--gray-400);
    margin: 3rem 0;
  }

  /* Music Section */
  .music-section {
    background-color: var(--white);
  }

  .platforms {
    display: grid;
    gap: 0;
  }

  .platform-link {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 0;
    border-bottom: 1px solid var(--gray-200);
    text-decoration: none;
    color: var(--black);
    transition: all 0.3s ease;
  }

  .platform-link:first-child {
    border-top: 1px solid var(--gray-200);
  }

  .platform-name {
    font-size: 1.5rem;
    font-weight: 400;
    transition: transform 0.3s ease;
  }

  .platform-link:hover .platform-name {
    transform: translateX(10px);
  }

  .arrow-icon {
    width: 24px;
    height: 24px;
    transition: transform 0.3s ease;
  }

  .platform-link:hover .arrow-icon {
    transform: translate(4px, -4px);
  }

  /* Contact Section */
  .contact-section {
    background-color: var(--gray-100);
  }

  .social-links {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1px;
    background: var(--gray-200);
    border: 1px solid var(--gray-200);
    margin-bottom: 6rem;
  }

  .social-link {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;
    background: var(--white);
    text-decoration: none;
    color: var(--black);
    font-size: 1rem;
    font-weight: 400;
    transition: all 0.3s ease;
  }

  .social-link:hover {
    background: var(--black);
    color: var(--white);
  }

  .social-link:hover .arrow-icon {
    transform: translate(4px, -4px);
  }

  .social-link .arrow-icon {
    width: 20px;
    height: 20px;
    transition: transform 0.3s ease;
  }

  /* Newsletter */
  .newsletter {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .newsletter h3 {
    font-size: 1.5rem;
    font-weight: 400;
    margin-bottom: 1rem;
  }

  .newsletter p {
    color: var(--gray-600);
    margin-bottom: 2rem;
    font-size: 1rem;
  }

  .newsletter-form {
    display: flex;
    gap: 0;
    border: 1px solid var(--gray-200);
    background: var(--white);
    max-width: 500px;
    margin: 0 auto;
  }

  .newsletter-form input {
    flex: 1;
    padding: 1rem 1.5rem;
    border: none;
    font-size: 1rem;
    font-family: 'Inter', sans-serif;
    font-weight: 300;
    background: transparent;
  }

  .newsletter-form input:focus {
    outline: none;
  }

  .newsletter-form button {
    padding: 1rem 2rem;
    background: var(--black);
    color: var(--white);
    border: none;
    font-size: 1rem;
    font-weight: 400;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: 'Inter', sans-serif;
  }

  .newsletter-form button:hover {
    background: var(--gray-600);
  }

  /* Footer */
  .footer {
    padding: 3rem 0;
    background: var(--white);
    border-top: 1px solid var(--gray-200);
  }

  .footer p {
    color: var(--gray-600);
    font-size: 0.875rem;
    text-align: center;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .style-switcher {
      flex-wrap: wrap;
      padding: 8px 12px;
      gap: 8px;
      font-size: 0.75rem;
    }

    .switcher-label {
      width: 100%;
      text-align: center;
      font-size: 0.75rem;
    }

    .style-switcher a {
      font-size: 0.75rem;
    }

    .hero h1 {
      font-size: 2.5rem;
    }

    .logo {
      width: 150px;
    }

    h2 {
      font-size: 2rem;
    }

    section {
      padding: 4rem 0;
    }

    .text-content .lead {
      font-size: 1.25rem;
    }

    .text-content p {
      font-size: 1rem;
    }

    .platform-name {
      font-size: 1.125rem;
    }

    .social-links {
      grid-template-columns: 1fr;
    }

    .newsletter-form {
      flex-direction: column;
    }
  }
</style>

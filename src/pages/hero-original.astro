---
import { socialLinks, musicPlatforms } from '../data/socialLinks.js';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="sunkissed - Indie Rock Band from San Francisco" />
    <link rel="icon" type="image/png" href="/logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Dela+Gothic+One&family=Josefin+Sans:wght@400;600;700&display=swap" rel="stylesheet" />
    <title>sunkissed - Indie Rock Band</title>
  </head>
  <body>
    <!-- Halftone Overlay -->
    <div class="halftone-overlay"></div>

    <!-- Psychedelic Sun -->
    <div class="psychedelic-sun"></div>

    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <div class="logo-badge">
          <img src="/logo.png" alt="Sunkissed" class="logo" />
          <div class="badge-ring"></div>
          <div class="badge-ring-inner"></div>
        </div>

        <h1 class="hero-title">
          <span class="title-main">SUNKISSED</span>
        </h1>

        <p class="hero-tagline">
          ★ INDIE ROCK FROM SAN FRANCISCO ★
        </p>

        <div class="hero-cta">
          <a href="#music" class="btn-vintage"><span>LISTEN NOW</span></a>
          <a href="#about" class="btn-vintage-outline"><span>OUR STORY</span></a>
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section id="about" class="about-section">
      <div class="container">
        <div class="section-header">
          <div class="wavy-title">
            <h2 class="section-title">THE STORY</h2>
          </div>
        </div>

        <div class="about-layout">
          <div class="about-image">
            <div class="image-frame">
              <img src="/cover_photo.jpeg" alt="Sunkissed Band" />
              <div class="frame-starburst"></div>
            </div>
            <div class="image-label">EST. 2024</div>
          </div>

          <div class="about-text">
            <div class="text-block">
              <p class="lead-text">
                ☀ THE BEGINNING ☀
              </p>
              <p>
                sunkissed started as a move — not a band. Two friends packed up their lives in Toronto and landed in San Francisco. It wasn't until six months later—after soaking in the Californian sun and feeling the late-night breeze—the music started flowing. What emerged was a hazy blend of indie rock, surf-tinged guitars, dreamy bedroom pop, and the occasional psychedelic drift. Now a four-piece, sunkissed makes music that feels like sun-faded memories—nostalgic, a little restless, and entirely their own.
              </p>
            </div>

            <div class="text-divider">
              <span class="star">✦</span>
              <span class="star">✦</span>
              <span class="star">✦</span>
            </div>

            <div class="text-block">
              <p>
                Their debut single, <strong>"On the Sun Kissed Hills of San Francisco,"</strong> is a raw, honest reflection on leaving Toronto for San Francisco, capturing the emotions felt while living in a new city. The song emerged from a simple guitar riff during a moment of inspiration while watching sunlight pour across hills of San Franciscan houses. Living just minutes from Haight-Ashbury—the birthplace of '60s counterculture—the track soaked up the neighborhood's storied spirit, blending it with their evolving connection to the city.
              </p>
              <p>
                Their newest single, <strong>Echos</strong>, is an energetic track that contrasts the tension between the joy of experiencing something new with the nostalgia of letting go of the past. The sound is dreamy, yet a little hazy, like a memory slowly fading away.
              </p>
              <p>
                With a new EP on the horizon, sunkissed continues to evolve their sound—leaning deeper into the tones and textures that make their music feel like both a memory and a moment.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Music Section -->
    <section id="music" class="music-section">
      <div class="container">
        <div class="section-header">
          <div class="wavy-title">
            <h2 class="section-title">LISTEN</h2>
          </div>
        </div>

        <div class="platforms-list">
          {musicPlatforms.map((platform, index) => (
            <a
              href={platform.url}
              target="_blank"
              rel="noopener noreferrer"
              class="platform-item"
            >
              <span class="platform-num">0{index + 1}</span>
              <span class="platform-divider">★</span>
              <span class="platform-name">{platform.name.toUpperCase()}</span>
              <span class="platform-arrow">→</span>
            </a>
          ))}
        </div>

        <div class="video-section">
          <h3 class="video-title">★ LATEST VIDEO ★</h3>
          <div class="video-container">
            <iframe
              src="https://www.youtube.com/embed/zNSEwreNbI0?start=23"
              title="On the Sun Kissed Hills of San Francisco"
              frameborder="0"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
              allowfullscreen
            ></iframe>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact-section">
      <div class="container">
        <div class="section-header">
          <div class="wavy-title">
            <h2 class="section-title">CONNECT</h2>
          </div>
        </div>

        <div class="contact-grid">
          <div class="social-panel">
            <h3 class="panel-title">★ FOLLOW US ★</h3>
            <div class="social-stack">
              {socialLinks.filter(l => ['Instagram', 'TikTok'].includes(l.name)).map((link) => (
                <a
                  href={link.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="social-link"
                >
                  <svg viewBox="0 0 24 24" class="social-icon">
                    <path d={link.icon} fill="currentColor" />
                  </svg>
                  <span>{link.name.toUpperCase()}</span>
                </a>
              ))}
            </div>
            <div class="email-contact">
              <p><strong>Email:</strong> <a href="mailto:hello@sunkissedband.com">hello@sunkissedband.com</a></p>
            </div>
          </div>

          <div class="newsletter-panel">
            <h3 class="panel-title">★ NEWSLETTER ★</h3>
            <p class="newsletter-desc">
              Sign up for our newsletter to get the latest news, releases, and tour announcements.
            </p>
            <form name="newsletter" method="POST" data-netlify="true" class="newsletter-form">
              <input type="email" name="email" placeholder="YOUR EMAIL" required />
              <button type="submit" class="btn-vintage"><span>SUBSCRIBE</span></button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <footer class="footer">
      <div class="container">
        <div class="footer-content">
          <p class="copyright">© 2024 SUNKISSED</p>
          <p class="footer-tag">☮ PEACE & MUSIC ☮</p>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --psychedelic-orange: #FF5500;
    --electric-purple: #8B00FF;
    --acid-yellow: #FFE600;
    --hot-coral: #FF6B6B;
    --deep-teal: #00897B;
    --burnt-umber: #3D0C02;
    --cream-paper: #FFF5E6;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Josefin Sans', sans-serif;
    background-color: var(--cream-paper);
    color: var(--burnt-umber);
    line-height: 1.7;
    position: relative;
    overflow-x: hidden;
  }

  /* Halftone Overlay */
  .halftone-overlay {
    position: fixed;
    inset: 0;
    background: radial-gradient(
      circle,
      var(--burnt-umber) 1px,
      transparent 1px
    );
    background-size: 8px 8px;
    opacity: 0.06;
    mix-blend-mode: multiply;
    pointer-events: none;
    z-index: 9998;
  }

  /* Psychedelic Sun */
  .psychedelic-sun {
    position: fixed;
    top: -30%;
    right: -20%;
    width: 80vw;
    height: 80vw;
    max-width: 800px;
    max-height: 800px;
    background: repeating-radial-gradient(
      circle at center,
      var(--psychedelic-orange) 0px,
      var(--psychedelic-orange) 25px,
      var(--acid-yellow) 25px,
      var(--acid-yellow) 50px,
      var(--hot-coral) 50px,
      var(--hot-coral) 75px,
      var(--electric-purple) 75px,
      var(--electric-purple) 100px
    );
    border-radius: 50%;
    opacity: 0.3;
    animation: sun-pulse 8s ease-in-out infinite;
    z-index: -1;
  }

  @keyframes sun-pulse {
    0%, 100% { transform: scale(1); opacity: 0.3; }
    50% { transform: scale(1.05); opacity: 0.4; }
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    background: linear-gradient(180deg, var(--cream-paper) 0%, #FFE4C4 100%);
    padding: 4rem 2rem;
  }

  .hero-content {
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .logo-badge {
    position: relative;
    display: inline-block;
    margin-bottom: 2rem;
  }

  .logo {
    width: 200px;
    height: auto;
    position: relative;
    z-index: 2;
    filter: sepia(20%) contrast(110%);
  }

  .badge-ring {
    position: absolute;
    inset: -20px;
    border: 6px solid var(--burnt-umber);
    border-radius: 50%;
    animation: badge-wobble 5s ease-in-out infinite;
  }

  .badge-ring-inner {
    position: absolute;
    inset: -10px;
    border: 3px dashed var(--psychedelic-orange);
    border-radius: 50%;
    animation: badge-spin 20s linear infinite;
  }

  @keyframes badge-wobble {
    0%, 100% { transform: rotate(-3deg); }
    50% { transform: rotate(3deg); }
  }

  @keyframes badge-spin {
    to { transform: rotate(360deg); }
  }

  .hero-title {
    margin-bottom: 1.5rem;
  }

  .title-main {
    font-family: 'Anton', sans-serif;
    font-size: 10rem;
    line-height: 0.9;
    letter-spacing: 0.02em;
    color: var(--acid-yellow);
    -webkit-text-stroke: 6px var(--burnt-umber);
    paint-order: stroke fill;
    text-shadow:
      8px 8px 0 var(--psychedelic-orange),
      16px 16px 0 var(--electric-purple);
    display: block;
  }

  .hero-tagline {
    font-family: 'Dela Gothic One', sans-serif;
    font-size: 1.2rem;
    letter-spacing: 6px;
    color: var(--burnt-umber);
    margin-bottom: 3rem;
    animation: text-melt 6s ease-in-out infinite;
  }

  @keyframes text-melt {
    0%, 100% { letter-spacing: 6px; transform: scaleY(1); }
    50% { letter-spacing: 10px; transform: scaleY(1.05); }
  }

  .hero-cta {
    display: flex;
    gap: 2rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-vintage,
  .btn-vintage-outline {
    font-family: 'Anton', sans-serif;
    font-size: 1.3rem;
    text-transform: uppercase;
    letter-spacing: 3px;
    padding: 20px 50px;
    text-decoration: none;
    transition: all 0.3s ease;
    cursor: pointer;
    position: relative;
  }

  .btn-vintage {
    background: var(--acid-yellow);
    color: var(--burnt-umber);
    border: 4px solid var(--burnt-umber);
    box-shadow:
      6px 6px 0 var(--psychedelic-orange),
      12px 12px 0 var(--hot-coral);
  }

  .btn-vintage:hover {
    transform: translate(-4px, -4px);
    box-shadow:
      10px 10px 0 var(--psychedelic-orange),
      20px 20px 0 var(--hot-coral);
  }

  .btn-vintage-outline {
    background: transparent;
    color: var(--burnt-umber);
    border: 4px solid var(--burnt-umber);
    box-shadow: 6px 6px 0 var(--burnt-umber);
  }

  .btn-vintage-outline:hover {
    background: var(--burnt-umber);
    color: var(--cream-paper);
    transform: translate(-4px, -4px);
    box-shadow: 10px 10px 0 var(--psychedelic-orange);
  }

  /* Section Styles */
  section {
    padding: 6rem 0;
    position: relative;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .wavy-title {
    display: inline-block;
  }

  .section-title {
    font-family: 'Anton', sans-serif;
    font-size: 5rem;
    letter-spacing: 0.05em;
    color: var(--burnt-umber);
    text-shadow: 4px 4px 0 var(--acid-yellow);
  }

  /* About Section */
  .about-section {
    background: var(--cream-paper);
  }

  .about-layout {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 4rem;
    align-items: start;
  }

  .image-frame {
    position: relative;
    border: 6px solid var(--burnt-umber);
    padding: 10px;
    background: var(--cream-paper);
    box-shadow: 8px 8px 0 var(--psychedelic-orange);
  }

  .image-frame img {
    width: 100%;
    height: auto;
    display: block;
    filter: sepia(15%) contrast(105%);
  }

  .frame-starburst {
    position: absolute;
    top: -30px;
    right: -30px;
    width: 80px;
    height: 80px;
    background: var(--acid-yellow);
    clip-path: polygon(
      50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%,
      50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%
    );
    display: flex;
    align-items: center;
    justify-content: center;
    animation: starburst-spin 10s linear infinite;
  }

  @keyframes starburst-spin {
    to { transform: rotate(360deg); }
  }

  .image-label {
    position: absolute;
    bottom: -20px;
    left: 50%;
    transform: translateX(-50%);
    font-family: 'Anton', sans-serif;
    font-size: 1.2rem;
    letter-spacing: 4px;
    color: var(--cream-paper);
    background: var(--burnt-umber);
    padding: 8px 20px;
  }

  .text-block p {
    font-size: 1.15rem;
    line-height: 1.9;
    margin-bottom: 1.5rem;
    color: var(--burnt-umber);
  }

  .lead-text {
    font-family: 'Dela Gothic One', sans-serif;
    font-size: 1.3rem !important;
    color: var(--psychedelic-orange) !important;
    text-align: center;
    margin-bottom: 2rem !important;
  }

  .text-block strong {
    color: var(--electric-purple);
    font-weight: 700;
  }

  .text-divider {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin: 2rem 0;
  }

  .star {
    color: var(--psychedelic-orange);
    font-size: 1.5rem;
    animation: star-pulse 2s ease-in-out infinite;
  }

  .star:nth-child(2) { animation-delay: 0.3s; }
  .star:nth-child(3) { animation-delay: 0.6s; }

  @keyframes star-pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.3); opacity: 0.7; }
  }

  /* Music Section */
  .music-section {
    background: var(--burnt-umber);
    color: var(--cream-paper);
  }

  .music-section .section-title {
    color: var(--acid-yellow);
    text-shadow: 4px 4px 0 var(--psychedelic-orange);
  }

  .platforms-list {
    max-width: 700px;
    margin: 0 auto 4rem;
  }

  .platform-item {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding: 2rem;
    border: 4px solid var(--acid-yellow);
    margin-bottom: -4px;
    text-decoration: none;
    color: var(--cream-paper);
    transition: all 0.3s ease;
    background: var(--burnt-umber);
  }

  .platform-item:hover {
    background: var(--acid-yellow);
    color: var(--burnt-umber);
    transform: translateX(10px);
    box-shadow: -10px 0 0 var(--psychedelic-orange);
  }

  .platform-num {
    font-family: 'Anton', sans-serif;
    font-size: 2rem;
    color: var(--acid-yellow);
    width: 50px;
  }

  .platform-item:hover .platform-num {
    color: var(--burnt-umber);
  }

  .platform-divider {
    color: var(--psychedelic-orange);
  }

  .platform-name {
    font-family: 'Anton', sans-serif;
    font-size: 1.8rem;
    letter-spacing: 3px;
    flex: 1;
  }

  .platform-arrow {
    font-size: 2rem;
    color: var(--psychedelic-orange);
    transition: transform 0.3s ease;
  }

  .platform-item:hover .platform-arrow {
    transform: translateX(10px);
    color: var(--burnt-umber);
  }

  /* Video Section */
  .video-section {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .video-title {
    font-family: 'Dela Gothic One', sans-serif;
    font-size: 1.3rem;
    letter-spacing: 3px;
    color: var(--acid-yellow);
    margin-bottom: 2rem;
  }

  .video-container {
    position: relative;
    width: 100%;
    padding-bottom: 56.25%;
    height: 0;
    overflow: hidden;
    border: 6px solid var(--acid-yellow);
    box-shadow: 8px 8px 0 var(--psychedelic-orange);
  }

  .video-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  /* Contact Section */
  .contact-section {
    background: var(--cream-paper);
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 3rem;
  }

  .social-panel,
  .newsletter-panel {
    border: 4px solid var(--burnt-umber);
    padding: 2.5rem;
    background: var(--cream-paper);
  }

  .panel-title {
    font-family: 'Dela Gothic One', sans-serif;
    font-size: 1.1rem;
    letter-spacing: 3px;
    color: var(--psychedelic-orange);
    margin-bottom: 2rem;
    text-align: center;
  }

  .social-stack {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.5rem;
    border: 3px solid var(--burnt-umber);
    text-decoration: none;
    color: var(--burnt-umber);
    font-family: 'Anton', sans-serif;
    font-size: 1.2rem;
    letter-spacing: 2px;
    transition: all 0.3s ease;
  }

  .social-link:hover {
    background: var(--acid-yellow);
    transform: translateX(5px);
    box-shadow: 5px 5px 0 var(--burnt-umber);
  }

  .social-icon {
    width: 28px;
    height: 28px;
    color: var(--psychedelic-orange);
  }

  .email-contact {
    margin-top: 2rem;
    padding-top: 2rem;
    border-top: 3px dashed var(--burnt-umber);
    text-align: center;
  }

  .email-contact p {
    font-size: 1rem;
    color: var(--burnt-umber);
  }

  .email-contact a {
    color: var(--electric-purple);
    text-decoration: underline;
    font-weight: 600;
  }

  .email-contact a:hover {
    color: var(--psychedelic-orange);
  }

  .newsletter-panel {
    box-shadow: 8px 8px 0 var(--acid-yellow);
  }

  .newsletter-desc {
    font-size: 1.1rem;
    line-height: 1.7;
    margin-bottom: 2rem;
    text-align: center;
    color: var(--burnt-umber);
  }

  .newsletter-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .newsletter-form input {
    padding: 16px 20px;
    font-family: 'Josefin Sans', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 2px;
    border: 3px solid var(--burnt-umber);
    background: var(--cream-paper);
    color: var(--burnt-umber);
  }

  .newsletter-form input:focus {
    outline: none;
    border-color: var(--psychedelic-orange);
    box-shadow: 4px 4px 0 var(--acid-yellow);
  }

  .newsletter-form input::placeholder {
    color: var(--burnt-umber);
    opacity: 0.5;
  }

  /* Footer */
  .footer {
    padding: 3rem 0;
    background: var(--burnt-umber);
    border-top: 6px solid var(--acid-yellow);
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-family: 'Anton', sans-serif;
    font-size: 1rem;
    letter-spacing: 2px;
    color: var(--cream-paper);
  }

  .footer-tag {
    color: var(--acid-yellow);
  }

  /* Reduced Motion */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .title-main {
      font-size: 4rem;
      -webkit-text-stroke: 3px var(--burnt-umber);
      text-shadow:
        4px 4px 0 var(--psychedelic-orange),
        8px 8px 0 var(--electric-purple);
    }

    .logo {
      width: 150px;
    }

    .section-title {
      font-size: 3rem;
    }

    .about-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .image-frame {
      max-width: 300px;
      margin: 0 auto;
    }

    .contact-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
      gap: 1.5rem;
    }

    .btn-vintage,
    .btn-vintage-outline {
      width: 100%;
      max-width: 280px;
      text-align: center;
      padding: 16px 30px;
    }

    .btn-vintage {
      box-shadow:
        4px 4px 0 var(--psychedelic-orange),
        8px 8px 0 var(--hot-coral);
    }

    .platform-name {
      font-size: 1.4rem;
    }

    .footer-content {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }

    .psychedelic-sun {
      width: 100vw;
      height: 100vw;
      top: -40%;
      right: -40%;
    }

    .video-container {
      box-shadow: 4px 4px 0 var(--psychedelic-orange);
    }
  }
</style>
